package com.eric.securechat.model;

/**
 * Enum representing friendship relationship status.
 * Defines the possible states of a friendship between users.
 */
public enum FriendshipStatus {
    PENDING,
    ACCEPTED,
    DECLINED,
    BLOCKED
}